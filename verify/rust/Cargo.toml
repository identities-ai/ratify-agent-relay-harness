[package]
name = "ratify-phase2-verify"
version = "0.0.0"
edition = "2021"
publish = false

[[bin]]
name = "verify_one"
path = "src/main.rs"

[dependencies]
# Local workspace SDK. The published reproduction pins the alpha.16 tag once cut.
ratify-protocol = { path = "../../../ratify-protocol/sdks/rust" }
serde_json = "1"
